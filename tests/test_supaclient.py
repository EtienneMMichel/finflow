import asyncio
from finflow.utils import SupaClient

# async def test_login_supabase():
#     client = SupaClient(supabase_key, supabase_url)
#     await client.login(username, password)

# async def test_set_data_supabase():
#     client = SupaClient(supabase_key, supabase_url)
#     await client.set_data("test_table", [{"name":"1", "surname":"1"},{"name":"2", "surname":"2"}])

# if __name__ == "__main__":
#     asyncio.run(test_set_data_supabase())